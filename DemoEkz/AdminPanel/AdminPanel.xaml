<Window x:Class="DemoEkz.AdminPanel.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DemoEkz.AdminPanel"
        mc:Ignorable="d"
        Title="AdminPanel" Height="700" Width="850">

    <Window.Resources>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="10 0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="8 0"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14 0"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <Border Background="White"
                CornerRadius="22"
                Padding="28">

            <Border.Effect>
                <DropShadowEffect BlurRadius="25"
                                  ShadowDepth="0"
                                  Opacity="0.18"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <StackPanel Grid.Row="0" Margin="0 0 0 22">
                    <TextBlock Text="Рабочий стол администратора"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="#0F172A"
                               HorizontalAlignment="Center"/>

                    <TextBlock Text="Добавление, изменение пользователей и снятие блокировки"
                               FontSize="15"
                               Foreground="#64748B"
                               HorizontalAlignment="Center"
                               Margin="0 8 0 0"/>
                </StackPanel>

                <!-- Добавление нового пользователя -->
                <Border Grid.Row="1"
                        Background="#F8FAFC"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="20"
                        Margin="0 0 0 20">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Добавление нового пользователя"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#0F172A"
                                   Margin="0 0 0 16"/>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Логин"
                                           FontWeight="SemiBold"
                                           Foreground="#334155"
                                           Margin="0 0 0 6"/>
                                <TextBox x:Name="txtNewLogin"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Пароль"
                                           FontWeight="SemiBold"
                                           Foreground="#334155"
                                           Margin="0 0 0 6"/>
                                <TextBox x:Name="txtNewPassword"/>
                            </StackPanel>

                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Роль"
                                           FontWeight="SemiBold"
                                           Foreground="#334155"
                                           Margin="0 0 0 6"/>
                                <ComboBox x:Name="cmbNewRole" VerticalContentAlignment="Center">
                                    <ComboBoxItem Content="Администратор" Tag="1"/>
                                    <ComboBoxItem Content="Пользователь" Tag="2"/>
                                </ComboBox>
                            </StackPanel>

                            <Button Grid.Column="6"
                                    x:Name="btnAddUser"
                                    Content="Добавить"
                                    Width="140"
                                    Background="#2563EB"
                                    Foreground="White"
                                    VerticalAlignment="Bottom"
                                    Click="btnAddUser_Click"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Таблица пользователей -->
                <Border Grid.Row="2"
                        Background="#F8FAFC"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="20">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0"
                                   Text="Список пользователей"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#0F172A"
                                   Margin="0 0 0 16"/>

                        <DataGrid x:Name="dataGridUsers"
                                  Grid.Row="1"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  SelectionMode="Single"
                                  SelectionUnit="FullRow"
                                  FontSize="14"
                                  RowHeight="38"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal"
                                  Background="White"
                                  BorderBrush="#CBD5E1"
                                  BorderThickness="1">

                            <DataGrid.Columns>

                                <DataGridTextColumn Header="ID"
                                                    Binding="{Binding Id}"
                                                    IsReadOnly="True"
                                                    Width="70"/>

                                <DataGridTextColumn Header="Логин"
                                                    Binding="{Binding Login}"
                                                    Width="*"/>

                                <DataGridTextColumn Header="Пароль"
                                                    Binding="{Binding Password}"
                                                    Width="*"/>

                                <DataGridComboBoxColumn Header="Роль"
                                                        SelectedItemBinding="{Binding Role.Name}"
                                                        Width="170">
                                    <DataGridComboBoxColumn.ItemsSource>
                                        <x:Array Type="{x:Type sys:String}"
                                                 xmlns:sys="clr-namespace:System;assembly=mscorlib">
                                            <sys:String>Администратор</sys:String>
                                            <sys:String>Пользователь</sys:String>
                                        </x:Array>
                                    </DataGridComboBoxColumn.ItemsSource>
                                </DataGridComboBoxColumn>

                                <DataGridCheckBoxColumn Header="Заблокирован"
                                                        Binding="{Binding IsBlocked}"
                                                        Width="130"/>

                                <DataGridTextColumn Header="Ошибок"
                                                    Binding="{Binding FailledAttemp}"
                                                    Width="100"/>

                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Кнопки управления -->
                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0 22 0 0">

                    <Button x:Name="btnRefresh"
                            Content="Обновить список"
                            Width="170"
                            Background="#E2E8F0"
                            Foreground="#0F172A"
                            Margin="0 0 12 0"
                            Click="btnRefresh_Click"/>

                    <Button x:Name="btnSaveSelectedUser"
                            Content="Сохранить изменения"
                            Width="210"
                            Background="#16A34A"
                            Foreground="White"
                            Margin="0 0 12 0"
                            Click="btnSaveSelectedUser_Click"/>

                    <Button x:Name="btnUnblockUser"
                            Content="Снять блокировку"
                            Width="190"
                            Background="#F59E0B"
                            Foreground="White"
                            Margin="0 0 12 0"
                            Click="btnUnblockUser_Click"/>

                    <Button x:Name="btnExit"
                            Content="Выйти"
                            Width="130"
                            Background="#EF4444"
                            Foreground="White"
                            Click="btnExit_Click"/>

                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>